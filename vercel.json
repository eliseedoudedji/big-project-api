{
  "installCommand": "npm install",
  "buildCommand": "npx prisma migrate deploy && npm run build",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
